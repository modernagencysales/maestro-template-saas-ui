// Cucumber compatibility adapter. The runtime implementation lives with the
// Playwright acceptance support so both acceptance systems share one backend.
export * from "../../tests/acceptance/support/runtime";
