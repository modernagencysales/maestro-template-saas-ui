{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://maestro.local/schemas/maestro-verification-receipt.schema.json",
  "title": "Maestro verification receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "createdAt",
    "command",
    "subject",
    "fingerprints",
    "scope",
    "gates"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "createdAt": { "type": "string", "format": "date-time" },
    "command": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "version"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "version": { "const": 1 }
      }
    },
    "subject": {
      "type": "object",
      "additionalProperties": false,
      "required": ["commit", "dirty"],
      "properties": {
        "commit": { "type": "string", "minLength": 1 },
        "dirty": { "type": "boolean" }
      }
    },
    "fingerprints": {
      "type": "object",
      "additionalProperties": false,
      "required": ["environment", "providerPosture"],
      "properties": {
        "environment": { "type": "string", "minLength": 1 },
        "providerPosture": { "type": "string", "minLength": 1 }
      }
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "changedPaths", "partial"],
      "properties": {
        "kind": { "enum": ["full", "focused"] },
        "changedPaths": {
          "type": "array",
          "items": { "type": "string" }
        },
        "partial": { "type": "boolean" }
      }
    },
    "gates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "gateId",
          "posture",
          "evidenceClass",
          "status",
          "semanticRuleIds"
        ],
        "properties": {
          "gateId": { "type": "string", "minLength": 1 },
          "posture": { "enum": ["required", "advisory"] },
          "evidenceClass": {
            "enum": [
              "static",
              "behavioral",
              "runtime",
              "live-promotion",
              "advisory"
            ]
          },
          "status": {
            "enum": ["pass", "fail", "skipped", "unavailable"]
          },
          "semanticRuleIds": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}
