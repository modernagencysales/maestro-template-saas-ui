# SaaS UI transplant stream report

## Milestone

This milestone records the compatibility cleanup applied after the initial
workspace chassis transplant. It is intentionally not a completion claim: strict
web typecheck remains red.

## Fresh verification

Command:

```text
pnpm --dir apps/web exec tsc -p tsconfig.json --noEmit --pretty false
```

Result: exit 2, with 52 TypeScript diagnostics across 20 files. The remaining
diagnostics are adapter-contract work, concentrated in contacts and settings;
they are not hidden with compiler exemptions.

Remaining files:

- `apps/web/src/features/contacts/list/add-person-dialog.tsx`
- `apps/web/src/features/contacts/list/list-page.tsx`
- `apps/web/src/features/contacts/view/activities-panel.tsx`
- `apps/web/src/features/contacts/view/activity-timeline.tsx`
- `apps/web/src/features/contacts/view/contact-tags.tsx`
- `apps/web/src/features/getting-started/create-workspace.tsx`
- `apps/web/src/features/reports/metrics/metric.tsx`
- `apps/web/src/features/settings/account/account-notifications-page.tsx`
- `apps/web/src/features/settings/account/account-profile-page.tsx`
- `apps/web/src/features/settings/account/account-security-page.tsx`
- `apps/web/src/features/settings/billing/billing-page.tsx`
- `apps/web/src/features/settings/billing/billing-status.tsx`
- `apps/web/src/features/settings/billing/manage-billing-button.tsx`
- `apps/web/src/features/settings/billing/plans-page.tsx`
- `apps/web/src/features/settings/common/settings-sidebar.tsx`
- `apps/web/src/features/settings/members/members-list.tsx`
- `apps/web/src/features/settings/members/members-page.tsx`
- `apps/web/src/features/settings/tags/manage-tags.tsx`
- `apps/web/src/features/settings/workspace/workspace-settings-page.tsx`
- `apps/web/src/features/workspaces/invite/accept-invite-page.tsx`

## Completed in this milestone

- Added local chart and i18n compatibility seams, including currency-aware
  number formatting.
- Removed unsupported Chakra/Saas UI props from loading/settings/contact
  surfaces and adapted the split-page open/close contract.
- Corrected generated route IDs and route parameter objects for search and
  contacts links while preserving feature-level workspace parameters.
- Added modal confirmation compatibility fields and notification-center
  preference/toast input compatibility.
- Made billing fixtures conform to the pinned Pro billing contract without
  changing the upstream billing component structure.
- Kept exact optional property checking enabled.

## Known non-typecheck follow-up

The required foundation authority files (`docs/template/saas-ui-upstream.json`,
deviation/acceptance maps, and the foundation checker), complete registry
materializer authority, and final browser evidence/reporting still need to be
integrated or verified against the implementation plan.

## Integration dependency resolution (2026-08-12)

The compatibility fix aligns `apps/web` with the pinned Pro/Chakra type graph:

- Chakra `3.30.0` and Ark `5.30.0`;
- `@saas-ui/chakra-preset` `3.0.0-next.10` (the release exporting the generated
  recipe/slot variant types used by the closure);
- `@saas-ui/react` `3.0.0-next.51` and `@saas-ui-pro/react` `1.0.0-next.4`;
- React/ReactDOM `19.2.0`, exact React type pins `19.2.2`/`19.2.0`;
- TanStack Form `^1.33.2` and Zod `4.1.5`.

The dependency contract test fails on the prior graph and passes on this graph.
After the change, frozen install succeeds and the complete receipt-listed
closure has no TypeScript diagnostics: filtering the fresh web typecheck output
for `src/components` and `src/hooks` produces no matches. The remaining
diagnostics are outside the receipt boundary under `src/features/**`, plus the
pre-existing Convex ownership error at
`packages/convex/confect/workflows/_kit/ownership.ts:773`.

The integrated chassis had changed receipt-listed
`apps/web/src/hooks/use-open-state.ts` after the original receipt commit. The
pinned materializer was rerun to restore that file deterministically; no receipt
file was manually edited, and the foundation hash check now passes.

## Task 8 mandatory current-blueprint projection

The manifest/receipt-derived frontend foundation contains exactly **203 unique
paths**. The resulting current target plan contains 464 entries for the neutral
selection, 491 for `records-example`, and 511 for `workflow-automation`.

The 123 receipt-listed Pro files, starter compositions, registry config, shell,
theme, archetype fixtures, lint/provenance authorities, notices, and frontend
docs are projected for all three selections. Records and workflow remain the
only optional pattern groups.
