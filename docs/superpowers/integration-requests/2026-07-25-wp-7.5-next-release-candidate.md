# WP-7.5 Next Release Candidate Integration Request

Owner: root release integration controller

Trigger: only after WP-7.5 and all prerequisite work are integrated and the root
worktree has a final reviewed SHA. Do not use an Agent H leaf SHA as the release
source, and do not edit, replace, or append files under any existing
`releases/v*/` directory.

Requested root integration:

1. Run the reviewed release-candidate generator from the final integrated SHA.
   Let that workflow choose and create the next versioned candidate; this lane
   does not name, publish, tag, or deploy it.
2. Project `docs/template/agent-pack-privacy.md` from its current canonical
   public authority into the customer target. The generated bytes must equal the
   public document byte-for-byte.
3. Classify these operator-only paths as omitted from the new customer release:
   - `tooling/agent-pack/src/privacy/network-policy.json`
   - `tooling/agent-pack/src/privacy/networkPolicy.ts`
   - `tooling/agent-pack/src/privacy/nodeSupportBundleExporter.ts`
   - `tooling/agent-pack/src/privacy/privacy.canaries.test.ts`
   - `tooling/agent-pack/src/privacy/privacy.noNetwork.test.ts`
   - `tooling/agent-pack/src/privacy/privacy.supportBundle.test.ts`
   - `tooling/agent-pack/src/privacy/privacy.test.ts`
   - `tooling/agent-pack/src/privacy/runtimeNetworkInterceptor.mjs`
   - `tooling/agent-pack/src/privacy/support-bundle.schema.json`
   - `tooling/agent-pack/src/privacy/supportBundle.ts`
   - `tooling/agent-pack/src/privacy/supportBundleCommand.ts`
   - `apps/cli/src/factory/supportBundle.ts`
   - `apps/cli/src/factory/supportBundle.test.ts`
4. Keep `tooling/agent-pack/src/privacy/disclosure.ts` in the customer closure
   because the current create contract imports it. Keep support-bundle exports
   out of the generated customer agent-pack barrel and keep support-bundle
   export out of MCP.
5. Regenerate candidate manifests, projection provenance, checksums, and archive
   bindings from the final integrated SHA. Never copy mutable current bytes into
   alpha.1 provenance.

Required root evidence:

- existing alpha.1 manifest and blueprint checksums remain
  `532c0da941bce540648b38c4fb868a35b7f37ff9d2623ff5778cd922866168f6` and
  `0fa873605ff4ba2224c740a27fc8fe5fb5d65dae81086aa9f1a7aa18f5dc32d5`;
- a fresh scratch customer materialization contains the exact public privacy
  document and none of the operator-only paths above;
- the customer agent-pack and CLI typecheck without support internals; and
- the new candidate passes customer-production exclusions and the complete
  release verification gate before any tag or publication decision.
