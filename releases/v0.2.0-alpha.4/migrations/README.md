# v0.2.0-alpha.3 to v0.2.0-alpha.4 migration notes

This release advances the generated SaaS UI foundation and isolated feature
materialization. It does not require a data migration.

Rollback uses the required pre-upgrade Git commit or the immutable alpha.3 tag.
Existing customer data and table names are unchanged, so no data rollback
receipt is required.
